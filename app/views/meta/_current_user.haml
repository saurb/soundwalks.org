- if logged_in? && !current_user.nil? && current_user.id
	%meta{:name => 'current_user_id', :content => current_user.id}
	%meta{:name => 'current_user_login', :content => current_user.login}
	%meta{:name => 'current_user_name', :content => current_user.name}
